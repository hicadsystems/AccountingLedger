@model List<NavyAccountCore.Entities.sr_SchoolRecordControl>
@{ ViewData["Title"] = "ViewSchool";
    Layout = "~/Views/Shared/_SchoolLayout.cshtml";
}

< <div class="header">
    <h1 class="header-title">
        School Portal
    </h1>
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/Home/Index">Main</a></li>
            <li class="breadcrumb-item active" aria-current="page">School Fee</li>
        </ol>
    </nav>
</div>
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title"></h5>
            </div>


        </div>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title"> Migrate Session</h5>

            </div>
            @if (TempData["returnMessage"] != null)
            {
            <h3 style="color:seagreen">@TempData["returnMessage"].ToString()</h3>         
            }
        <div class="card-body">
            <div class="row">
                <div class="col-md-2">
                    <label>Current Session</label>
                    <div class="form-group">
                        <input type="text" class="form-control" value="@Model.FirstOrDefault().Period" readonly />
                    </div>

                </div>
                <div class="col-md-2">
                    <label>Current Term</label>
                    <div class="form-group">
                        <input type="text" class="form-control" value="@Model.FirstOrDefault().term" readonly />
                    </div>

                </div>
            </div>
            @using (Html.BeginForm("MigrateToSession", "SRSesssion", FormMethod.Post))
            {
        <div class="col-md-1">
            <div class="form-group">
                <input type="submit" class="btn btn-lg btn-primary" value="Migreate To New Session" />
            </div>

        </div>}

        </div>

        </div>
    </div>
</div>
